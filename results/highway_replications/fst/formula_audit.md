# Fluctuation formula audit

The reproduction keeps `fluctuation_weight: 0.0`. Direct substitution of the signed paper Eq. (24) gives `sum_i w_i F_i = mu_m - mu_hat_m`; therefore this term can duplicate the existing signed estimation error under a direct aggregation. No absolute value or variance replacement was introduced.

Maximum numerical identity residual across primary sets: `1.556e-09`.
