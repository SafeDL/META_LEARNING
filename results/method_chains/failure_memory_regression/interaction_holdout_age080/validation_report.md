# age080 独立验证结果

2026-09-26 按事前冻结的 `protocol.json` 和八个模拟种子 `4179910`–`4179917` 完成验证。每个种子有 128 个 IA/IB 候选，三个构建各执行一次，共 3072 个真实物理回合。并行执行仅按种子分配工作；首次串行运行留下的 109 个有效回合经逐行 JSON 校验后由原有断点续跑机制复用。完整测量库通过 `_bank` 的场景、构建、种子和指纹校验，24 个测量文件各有 128 行，无 inconclusive 回合。

| 构建 | 回合 | ego 碰撞 | 使用过期后车状态的回合 | 过期状态预测调用 | 发生换道的回合 |
|---|---:|---:|---:|---:|---:|
| `mobil_ref_v2` | 1024 | 0 | 0 | 0 | 618 |
| `mobil_rear_guard_off_v2` | 1024 | 25 | 0 | 0 | 1024 |
| `mobil_rear_state_age080` | 1024 | 0 | 1024 | 5713 | 754 |

历史故障构建的 25 次碰撞中，17 次是 IA 换道期间侧后车碰撞，6 次是 IB 同类碰撞，其余两次分别是 IA 后车碰撞和换道后前车碰撞。age080 的控制行为与参考构建不同，但未形成有效 ego 碰撞。因此八个种子的参考通过且目标失败的回归池均为 0。

按预定的每个种子 10 次选择器重复、每次 20 次查询，`FBRT-Memory`、`FBRT-NoMemory`、`HistoryRank-UCB-v2`、`FailureDistance-v2` 和 `Random` 的发现总数均为 0。八个物理种子的 Memory 减 NoMemory 差值均为 0；双侧精确符号翻转检验 `p=1.0`。预定的显著提升门槛 **未通过**。`analysis.json` 中的 `memory_overall_best_descriptive=true` 是零分并列产生的非严格比较，不代表方法优势。

这一次验证到此停止，不追加种子、改动 age 值或按结果过滤候选。要继续研究显著提升，首先需要一个独立且真实产生目标回归的构建或任务；新目标、候选、历史隔离和统计规则应在任何新物理执行之前另行冻结。这里的阴性结果不能用选择器调参解释为正结果。

复核入口：`protocol.json`（原始冻结协议）、`measured/`（完整物理测量库）、`evaluation/analysis.json`（门槛结果）、`evaluation/summary.jsonl` 和 `evaluation/queries.jsonl`（五种方法的全部运行）。
