extends: posterior_adaptation_preexperiment.yaml

# Reward normalization reduces the Bellman-loss scale. This validation-only
# pilot tests a correspondingly weaker information-bottleneck penalty without
# introducing task-label or hidden-rule supervision.
pearl:
  kl_beta: 0.001
