extends: posterior_adaptation_low_kl_preexperiment.yaml

# Validation-only representation pilot. Training labels supervise latent
# organization only; labels/task IDs are never policy or evaluation inputs.
task_representation:
  enabled: true
  latent_dims:
  - 2
  - 2
  - 1
  geometry_weight: 0.02
  interaction_weight: 0.02
  rule_weight: 0.1
