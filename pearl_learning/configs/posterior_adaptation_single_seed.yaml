extends: posterior_adaptation_protocol.yaml

# Compute-saving first-stage protocol requested for the sequential gate.
# All task, environment, reward, architecture, and budget settings are
# inherited unchanged; only the set of independent training seeds is reduced.
posterior_adaptation:
  training_seeds:
  - 11
