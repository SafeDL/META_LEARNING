# Matched vanilla Dense PEARL control for the method-flow pilot.
extends: merge_method_flow_pilot.yaml

project:
  output_root: results/pearl_learning/merge_method_flow_vanilla_pilot

scenario_representation:
  enabled: false

scenario_prior:
  mode: unit_normal

experiment:
  method_variant: vanilla
