extends: posterior_adaptation_disentangled_preexperiment.yaml

# The 0.1 rule weight learned only a constant class-prior decoder on
# validation. Increase the training-only rule organization pressure while
# keeping geometry/interaction regularizers unchanged.
task_representation:
  rule_weight: 1.0
