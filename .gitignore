# Local interpreter, test and editor caches
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd
.idea/
.DS_Store

# Local environments and credentials
.env
.env.*
!.env.example
.venv/
venv/

# Keep experiment results, logs, archives and model weights eligible for Git.
# Gitignore cannot filter by file size; add a specific path here only when an
# individual artifact is too large to store in the repository.
